
ssh pi-robot@ip

lần đầu tiên cần Đồng bộ biến môi trường ROS 2
Thực hiện các bước sau trên CẢ LAPTOP VÀ PI, Mở file cấu hình bashrc:
	sudo gedit ~/.bashrc   # (Hoặc dùng 'nano ~/.bashrc' nếu ở trên Pi)
Kéo xuống cuối cùng và dán 2 dòng sau vào:
	export ROS_DOMAIN_ID=0
	export ROS_LOCALHOST_ONLY=0

Lưu file và cập nhật hệ thống:
	source ~/.bashrc

1. KHỞI CHẠY (ROBOT) 
	tắt pi dùng: sudo poweroff
	
-----------------trên PI---------------------
cd tới ~/ros2_ws
colcon build
source install/setup.bash
ros2 launch my_robot robot.launch.py

-----------------trên Laptop-----------------
cd tới ~/robot1_ws
colcon build
source install/setup.bash
ros2 launch brain_pkg main.launch.py
